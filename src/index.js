import React from 'react';
import ReactDOM from 'react-dom';
import './index.css';
import MemeGenerator from './Scraper';

ReactDOM.render(
  <React.StrictMode>
    <MemeGenerator />
  </React.StrictMode>,
  document.getElementById('root'),
);
